"""Runtime shim for pyquotex adjustments.

This module exists to centralize any monkey patches or runtime tweaks needed
for the pyquotex stable API integration. It may be intentionally minimal when
no patches are required at runtime.
"""
